var usuario = prompt("ingrese el nombre del usuario");
var monto = parseInt(prompt("Ingrese el monto a transferir"));

if (usuario == "Pablo" && monto > 0) {
    alert("Transferencia Existosa");

} else {
    alert("transferencia denegada");
}