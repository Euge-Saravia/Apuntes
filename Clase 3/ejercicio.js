/* var mayor = 18; var suma = 0;
do {
	var num = parseInt(prompt('Ingresa tu edad'));
	validaredad(num);
} while (num !== 0)
function validaredad(num) {
	if (num > mayor) {
		suma++;
		return true;
	} else {
		return false;
	}
}
alert('La cantidad de mayores de edad es ' + suma); */

var digito = 9; suma = 0

do {
	var num = parseInt(prompt("Ingrese un numero"));
	validar(num);
} while (num <= 9);
function validar(num) {
	if (num < digito) {
		suma++;
		return true;
	} else {
		return false;
	}
}
var suma = 
alert ("El promedio es: " +);
